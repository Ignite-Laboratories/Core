package core

func ConfigureEnivornment() {
	// TODO: Load in a conifugration file
	// TODO: Allow the configuration file to override the name database
	// Do this by just directly assigning to the global nameDB variable,
	// this gets called before it's initialized into a NameDB struct.
}
